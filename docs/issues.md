# Issues register

Everything identified during design and review, with status. Issues found in **our** code, in the
**contract**, in **upstream** (LibLCM / FieldWorks / PanGloss), and in our own **tooling**. Upstream
hazards are recorded because we must defend against them, not because we can fix them.

Status: `open` · `in-progress` · `fixed` · `wontfix (recorded)` · `upstream`

---

## A. Defects in shipped Motif code

| # | Issue | Status |
| --- | --- | --- |
| A1 | **Store cannot express amend.** `objects/<changeSetId>.json` is keyed by the frozen id, so amending would mutate a supposedly immutable object. Fix is the git model: object keyed by `intentDigest` (write-once), manifest keyed by `changeSetId` holding a movable pointer. | fixed |
| A2 | **`apply` is unbound.** `ChangeSetApplier.Apply` takes no Assessment, so [ADR 0004](adr/0004-prerequisite-graph-stable-ids-bound-apply.md) §3's "a bare apply is a hard error" is unenforced and the assess→apply TOCTOU window is open. | fixed |
| A3 | **`RollbackCacheInvalidator` could commit a mutation.** It called `ILexEntryRepository.ResetHomographs` → `LexDb.ResetHomographNumbers`, which wraps a project-wide `HomographNumber` reassignment in `DoUsingNewOrCurrentUOW`. **Correction to my original framing:** at the *existing* call site (inside the applier's catch block) the outer unit of work is still open, so `CurrentDepth > 0` and it would have *joined* that bundle rather than committing separately — the defect was **latent, not actively firing**. The real hazard is any future call site invoking rollback-cleanup after the unit of work has closed (`CurrentDepth == 0`), which is the documented usage and takes the `else` branch that opens a new task and commits. Fixed unconditionally rather than relying on that call-site nuance: it no longer calls `ResetHomographs` at all, and instead marks the cache non-reusable — the only correct response given C15. **Superseded 2026-08-06:** the type is deleted. A failed apply's catch block now does nothing at all, so there is no cleanup call site left to get wrong, at any `CurrentDepth` (ADR 0016 as amended). | fixed (code deleted) |
| A4 | **Assess can poison derived caches.** Assess mutates then rolls back on every call, and `UndoStack.Rollback` (unlike `Undo`) skips the forward-only-hook cache invalidation, leaving headword / homograph / monomorphemic caches stale. Dormant today only because `setGloss` touches none of them; **fires the moment a lexeme-form or citation-form operation exists**, which is the next operation we add. A second assess on the same cache could then return a different effect digest for identical state — breaking the determinism clause and "re-run and confirm no change." **Resolved differently 2026-08-06, and better:** the guard is gone because the cause is gone. A Dry Run mutates a single-use file copy and discards it — it never rolls back, so it never poisons anything, and no field needs classifying. ADR 0016 as amended; the `AssessPoisonsCache` column, `DerivedCachePoisoningOperationKinds` and `CacheReusability` are all deleted. This answers `C10a`. | fixed (cause removed) |
| A5 | **CLI draft file contradicts the in-memory-draft decision.** `drafts/<name>.json` exists because each CLI verb is a separate OS process, but S3 says drafts never touch disk. Documented as a deliberate session shim; a library or daemon consumer must keep drafts in memory. | fixed (documented) |
| A6 | **Baseline retention could never delete a published Baseline.** `BaselineRetentionCleaner.Clean` called `File.Delete` on `PublishedBaseline.Path`, but publication produces a **directory** — `BaselineBundleReceiver` moves the extracted bundle to a digest-named directory holding the `.fwdata`, `WritingSystemStore/` and `SharedSettings/`. `File.Delete` against a directory throws, the surrounding catch recorded a cleanup failure, and every later pass failed identically, so superseded Baselines would have accumulated without bound. `DeleteDirectory` existed on the file-system seam and was never called from `Clean`. Invisible because every test built the Baseline as a plain file with `File.WriteAllText` — a shape publication never produces. **This is the same fixture-shape blindness as the `SharedSettings` mutation:** the assertion was fine, the fixture was the wrong shape. Fixed by walking the tree bottom-up and re-checking ownership and reparse points at every level, matching `WorkspaceCleaner.DeleteTree` and `ProjectWorkspaceEvictor.DeleteTree`; the tests now build the production shape, and one asserts the directory is genuinely gone. Latent when found — the cleaner had no production call site, and would have armed with the first retention wiring. Rule adopted: **when a test builds a fixture by hand, check that its shape is one production actually produces.** | fixed |
| A7 | **A crash between publishing a Baseline and recording it orphans the directory.** `BaselinePublishCommandHandler` moves the verified bundle into place before writing the durable row — the right order, artifact before the row that claims it — and an in-process failure is compensated by `DeletePublicationIfOwned`. But if the process dies in that window no compensation runs, and nothing afterwards reconciles: `ReclaimIncomingDirectories` sweeps only `.incoming-*` staging names, never a fully-moved digest directory, and no query can see a directory that has no row. That directory is then unreachable by retention forever, unless the identical content is republished under the same digest. The consequence is a disk leak, not corruption or a misleading status — nothing reports the orphan as current. Not fixed by choice: the repair is a startup reconciliation sweep over digest directories with no `Baselines` row, which belongs with the retention/archive work that is not yet built, and whose own plan defers exactly these queries to an archive-integration checkpoint. | open |
| A8 | **`HeadlessLcmUi.ConflictingSave` silently discards a conflicted save while reporting success.** It returns `true`, and `true` means revert: `UnitOfWorkService.GetUserInputOnConflictingSave` calls `m_ui.ConflictingSave()` and on `true` runs `RevertToSavedState()`, then `SaveInternal` returns normally with no exception and no signal to the caller. The class doc claims the opposite intent — "destructive/ambiguous decisions fail loudly (`NotImplementedException`) rather than silently guessing" — and every other ambiguous callback in the type does exactly that. **Latent, and only by luck of the backend:** the path is reachable only when a foreign peer commits between this process's read and its save, which the exclusive `{project}.fwdata.lock` taken by `XMLBackendProvider` makes impossible today. It would arm immediately if Motif ever opened a project as a `kSharedXML` peer. [ADR 0040](adr/0040-one-api-the-cli.md) decision 6 rules that out on exactly this ground, so the defect is contained by architecture rather than by code. Fixed: it now throws `NotSupportedException` like every other ambiguous callback in the type, naming what happened rather than reporting the call unimplemented. Containment is now belt-and-braces — architecture keeps the path unreachable, and the code fails where a caller can see it if that ever changes. Note the second answer is no safer than the first: `false` keeps changes LibLCM has already judged unreconcilable, which is why refusing is the only correct response for a process with no person to ask. | fixed |

## B. Contract and design gaps

| # | Issue | Status |
| --- | --- | --- |
| B1 | **Contract lags the ADRs.** The ten-verb vocabulary, generated kinds, two field spaces, declared-vs-discovered footprint, drift taxonomy and more live only in ADRs. The contract is what implementation follows. | fixed |
| B2 | **`ensure` is a required verb with no home.** Custom fields need tri-state semantics and crash-retry safety depends on it, but `create`'s idempotent-reuse path is keyed by **canonical GUID** while custom fields have **no durable GUID** and match on `(class, name)` — a different identity axis, previously unreconciled. | fixed |
| B3 | **Correction 2026-08-06:** the resolution rested on a false premise. Overwriting an `owning/atomic` slot **destroys** the displaced occupant rather than detaching it — measured with LibLCM's own factory (`DisplacedOccupantFactTests.cs`), verified for `LexEntry.LexemeForm`. So there is no orphan to disclose and no apply to refuse; `change-set-contract.md` had applied the reference-de-referencing hazard to ownership, where it guards nothing. Original finding: **Owning-atomic replacement undefined** across 69 in-scope fields — the everyday "change which allomorph is the lexeme form" edit. Without a rule, a raw `create` silently reintroduces the MSA-orphan bug class. | fixed (premise corrected 2026-08-06) |
| B4 | **Binary effect-digest equality is too coarse for approval continuity.** Needs the four computable drift classes plus a bulk-approvable path for same-nature-wider-scope. | fixed |
| B5 | **No `Info` outcome category.** "Checked and confirmed harmless" is currently conflated with "nothing needed checking." | fixed |
| B6 | **Dangling and engine-nulled references share one severity bucket.** A dangling reference is materially worse and must be distinguished. | fixed |
| B7 | **No Integer enum table.** The `(Kind, Card, Sig)` triple cannot distinguish a closed enumeration (`PhSegmentRule.Direction`, `MoAdhocProhib.Adjacency`) from a magnitude (`CmPicture.ScaleFactor`). 30 in-scope `Integer` fields would invite magic-number authoring. Needs a manifest column. | fixed |
| B8 | **Third comparison class missing.** Index-as-identity (alpha variables) is neither unordered, positional, nor feeding, and its reach is not neighbour-limited. | fixed |
| B9 | **Third ownership mode missing.** Pooled-but-private objects (`PhPhonData.Contexts`, `.FeatConstraints`) are a rule's private interior but live in a shared pool, so the fill/frame ownership test gives the wrong answer. Deleting the owner also orphans pool members that were never the delete's target. | fixed |
| B10 | **`reparent` confirmed only for `owning/seq`.** All three evidenced examples are sequences; atomic and collection reparent are plausible but unevidenced. Needs a conformance vector before being promised. | open |
| B11 | **Batch-scoped composers are unaddressed.** Bulk POS/inflection-feature assignment must see the whole selected batch grouped by owning entry to get MSA reuse right — not N independent composer calls. | open |
| B12 | **Writing systems and custom fields have no inventory row.** `LangProject.*Wss` are space-joined ID strings; both families are real but not derivable from the manifest. This is the open field space. **Split 2026-08-09 after checking the FieldWorks source, because both halves of this row rested on assumptions that failed.** *(a) Writing systems are grammar inputs, not configuration* — `PhCode.Representation` is `MultiUnicode` (`MasterLCModel.xml:4452`), `HCLoader` builds the character-definition table by reading it through `VernacularDefaultWritingSystem` (`HCLoader.cs:1398, 2679-2680`), loads the Valid Characters list as the segment inventory under `AcceptUnspecifiedGraphemes` (`:2718`), and **excludes any lexical entry whose form is empty in that writing system** (`:543, 585-586`). And `DefaultVernacularWritingSystem` is literally `FirstOrDefault()` over the ordered `CurVernWss` (`OverridesLangProj.cs:844-849`), so **reordering it changes the phoneme inventory and can silently shrink the grammar**. Addressed: `WritingSystemInventory` reads them in order with a digest, so the arrangement becomes part of what an Assessment was computed under; read-only by decision, since reordering also changes how every multilingual field in the project displays (ADR 0034's state/change boundary). *(b) Custom fields can reach a parser* — `XAmplePropertiesPreparer.cs:69-70` creates them programmatically on `LexEntry` and `MoForm` as possibility-list references and an augmenter writes their contents into XAmple's grammar and lexicon files; the FLEx dialog's restricted class list is a UI restriction, not an engine one. HermitCrab never reads a custom field, so this bites only for XAmple projects — **and whether those are in scope at all is unanswered**, which is what keeps this row open. | **fixed 2026-08-09 — closed by scope.** The owner's ruling: **XAmple is not in scope; HermitCrab is the target and Motif builds to it.** Custom fields reach a parser only through XAmple's augmenter, and `HCLoader` never reads one, so with XAmple out of scope no custom field can affect anything Motif measures. The writing-systems half was already addressed by `WritingSystemInventory`. **One factual caveat on the framing, which does not change the conclusion:** custom fields are the single place found where XAmple reads something HermitCrab does not, so "HC is a superset" is not strictly true in that one respect — it is true of everything in scope, which is what matters. |
| B13 | **Cross-process protocol unspecified.** Framing, error/exit-code contract, and one-shot-vs-daemon for the Python/Rust consumers. A hybrid was recommended (store ops one-shot; project ops via a per-project daemon that *is* the exclusive-write guarantee). **Reshaped 2026-07-27, still open.** Two constraints are now fixed: (a) [ADR 0012](adr/0012-build-order-hc-spine-first-kinds-generated.md)'s 915 generated kinds make typed CLI verbs impossible, so a generic change-set-JSON path is forced, not optional; (b) [ADR 0011](adr/0011-experiment-loop-boundary-motif-is-the-record.md)'s scratch-copy export weakens the daemon's *exclusive-write* rationale, since experiments touch throwaway copies. Its *cache-reuse* rationale strengthened instead — measured cache load is **10.1s cold / 3.6s warm on a 61-entry project**, against **0.05s** to copy the whole 43 MB project, and 7 of the 12 verbs need no cache at all. | open |
| B14 | **Resource/DoS bounds are a TODO with no numbers** for untrusted agent-authored change sets. | open |
| B15 | **Diagnostics i18n undecided** — language and localisability of human-readable explanations. | open |
| B16 | **"Generates no kind" conflated two different things** — truly unauthorable fields (engine-computed, residue, trace) with authorable-but-HC-inert ones (every grammar `Description`, the lexicographic apparatus). Since coverage is HCLoader-complete but Motif is still a general LibLCM runner, a field can be legitimate to author for a dictionary while having no effect on a parse. Split into two lists; the second keeps its kinds with `HcReachable=no`. | fixed |
| B17 | **`MoMorphSynAnalysis.Components`/`GlossBundle` classified with low confidence** — no documented guidance found either way; defaulted to `semantic-operation`. Needs confirmation against FieldWorks usage. **Corrected 2026-08-03:** the guidance exists and is explicit. `MasterLCModel.xml:3578-3584` — *"I have defined this as an ordered seq on the assumption that the order of the components will match the left-to-right order of the morphemes"*; `:3591-3596` for `GlossBundle`. Both classifications confirmed correct; B17 overstated its own uncertainty. | fixed |
| B18 | **~300 of 473 in-scope rows classified by field-name heuristic**, not an explicit citation. **Corrected 2026-08-03 ([audit](research/2026-08-03-manifest-trust-audit.md)): 406 of 473 (85.8%) carry zero citation**, only 27 cite a file *and* line; the real errors sat in the 61 non-default rows (~23% flawed when sampled). **Largely retired 2026-08-05 ([ADR 0022](adr/0022-structure-is-derived-policy-is-five-rows.md)).** The premise was that the generator *trusts* these rows. It no longer does: `Verbs` is a pure function of `Kind`/`Card` (7 combinations, zero exceptions, 412 rows) and `ComparisonClass` is `seq`→`positional` with **five** cited exceptions, so both are derived and the build fails on any unexplained departure. A missing citation on a computed value is not a risk. What survives is narrow — citations for the order-carries-meaning rows and for `Scope` decisions. **Measured 2026-08-11:** 64 in-scope rows claim order carries meaning (59 `positional`, 3 `index-as-identity`, 2 `feeding`), and 63 of them carried no citation of any kind. `manifest/ordering-evidence.tsv` now quotes, cites and digests what `MasterLCModel.xml` says about ordering for each — **32 have an explicit statement nobody had harvested** ("The order is from the innermost affix out", "in the order in which they are to be applied"), and **32 rest on `card=seq` alone** and are named in [the census](research/2026-08-11-ordering-claims-census.md) as the bounded review the 2026-08-03 audit recommended. | fixed |
| B19 | **RESOLVED 2026-08-05 ([ADR 0023](adr/0023-derived-kind-names-required-descriptions.md)):** the identifier is now **derived** as `lowerFirst(DeclaringClass)` with no prefix table, and human meaning moved to a required, never-hashed `description` seeded from FieldWorks' own labels. Prior art was decisive — Kubernetes, protobuf, OpenTelemetry, Salesforce and FHIR all keep an immutable machine identifier separate from a governed human label, and `Construct` had been doing both jobs with one string. The original finding, for the record: **Construct-to-kind-segment naming is not mechanical.** `LexSense.Gloss` carries `Construct=lexSense`, but the shipped kind is `lexical/lexSense/setGloss` — segment `sense`, not `lexSense`. The generator ([ADR 0012](adr/0012-build-order-hc-spine-first-kinds-generated.md)) cannot run unattended until the manifest's construct names are normalized or a mapping is committed. **Quantified 2026-08-03: understated.** Of 53 construct names, only **26.4%** are `lowerFirst(Class)`, 32.1% need a `Cm`/`Mo`/`Ph`/`Fs` prefix table that is not in the data, and **41.5% have no mechanical relationship to any class** (`featureStructure` spans 16 classes, `ruleContext` 11, `msa` 9). The `lexSense`→`sense` case is a *second* undocumented normalization on top, with no stated rule for when it applies. | fixed |
| B20 | **RESOLVED 2026-08-05 ([ADR 0023](adr/0023-derived-kind-names-required-descriptions.md)) — the ask was impossible, not merely unresolved.** Verified: **11 of the 20 possibility lists are created with the bare `CmPossibility` class** (`BootstrapNewLanguageProject.cs:80` against `:78`), so for the majority there *is* no concrete subclass to name. What distinguishes a Confidence Level from a Part of Speech is which list owns the object — a runtime fact. The pipe-separated cell was an attempt to put a runtime fact in a schema-time name. Fields are now named where they are **declared**, one kind serving all 13 descendants, and the list identity reaches the reviewer through the target object instead. The original finding: **17 authorable rows carry a multi-construct string** (`possibility\|partOfSpeech\|lexRefType\|…`) with no single kind namespace. Needs a resolution rule — most likely fan-out to one kind per construct — before generation is unambiguous. Generator prerequisite alongside B19. **Reconciled 2026-08-03:** 19 raw rows minus 2 `derived-read-only` (`DateCreated`, `DateModified`) = 17. And the fan-out resolution **cannot be done from `Class`/`Field` alone**: all 19 are plain structural fields with one meaning each; `CmPossibility` is one generic class reused as storage for seven lists, so the ambiguity is *which list instance owns the object at runtime* — not a schema fact. Adjacent to B12. | fixed |
| B21 | **CLOSED 2026-08-05.** The closure is computed *and* the query that defined L0 is retired — [ADR 0025](adr/0025-parser-first-build-order.md) replaces the L0 → G0–G2 → backfill order with a single parser-first slice, because 13 of L0's 37 fields are never read by `HCLoader` and populating the surviving 24 needs five grammar classes anyway. Original finding: **L0's object-creation closure is uncomputed.** [ADR 0012](adr/0012-build-order-hc-spine-first-kinds-generated.md) defines L0 by manifest query (`HcReachable=yes`, `Group!=grammar`, 37 fields), but a field being *read* by `HCLoader` does not mean an object can be *created* with only those fields set. **Computed 2026-08-03 ([findings](research/2026-08-03-five-computable-grill-items.md#b8)).** A minimal valid `LexEntry` needs 4 classes (`LangProject`→`LexDb`→`MoMorphType`→`LexEntry`, cascading `LexSense`+`MoForm`); full L0 population additionally pulls in `PhEnvironment`, `MoInflClass`, `PartOfSpeech`, `MoInflAffixSlot`, `FsFeatStruc` — all G0/G1, a cost ADR 0012 does not state. **And the query is unsound: 13 of the 37 fields are never read by `HCLoader.cs`**, two of them bare-name false positives (`MoMorphTypeTags.kguidMorphPrefix`, HermitCrab's `Direction.RightToLeft`) — the failure mode `HcReachable` exists to correct. See `B8a`. | fixed |
| B22 | **Reverse `Expand` lost its oracle.** [ADR 0011](adr/0011-experiment-loop-boundary-motif-is-the-record.md) deletes forward projection, which S6 had designated `Expand`'s round-trip oracle. `Expand` must instead be validated against HC's own conformance suite (`grammar.xml` + `words.yaml`, ground truth mechanically re-derived) — a different and unbuilt harness. | open |
| B23 | **Attachment/metric configuration surface is unspecified.** [ADR 0011](adr/0011-experiment-loop-boundary-motif-is-the-record.md) establishes labelled attachments, Markdown render designation, and typed metrics, all configuration-declared — but the config format, whether the label taxonomy is open or closed, and the metric type vocabulary are undecided. | open |
| B24 | **Drift protects what a Proposal changes, not the evidence it rests on.** A grammar Proposal is justified by a coverage number computed against texts it never touches. Sync in new texts (the recorded two-linguist workflow: one analyses text, hands off by Chorus, the other fits rules to it) and that number is stale while the footprint digest is untouched and reports the baseline as clean. The bound Dry Run anchor cannot see it, because the texts are outside the footprint by construction. Options, none chosen: widen the anchor to cover the corpus the assessment read; version the assessment against a corpus digest and re-run on mismatch; or accept it and label coverage numbers with the corpus state they were measured against. **Decided 2026-08-06** ([ADR 0032](adr/0032-stem-assessment-is-pangloss-supplied-lexicon.md) §4): the third, on the owner's call that "a coverage number should include the text used and a hash of them." A coverage figure is never a bare percentage — it cites the corpus, that corpus's hash, the supplied-lexicon overlay revision, and the grammar identity, so a stale number says so. PanGloss independently reached the same instinct, versioning its user-delta cache by user-lexicon hash. | fixed (decided) |
| B25 | **Does removing a destructive operation from a draft count as "consequences that cannot be enumerated"?** `MOT-18` as built refuses to remove any operation whose verb is `delete` — today exactly one kind, `lexical/lexEntry/deleteLexemeForm` — and makes that refusal unforceable, on the grounds that LibLCM's owning cascade reaches objects the Proposal never names. **I think that is a category error and have left it in place pending the owner's call.** The unenumerable reach is a consequence of *applying* the delete; the consequence of *removing* it is that the delete does not happen and nothing is destroyed — strictly less destructive, and the in-Proposal consequences (severed `requires` edges, orphaned operations) are all declared and enumerable. [ADR 0021](adr/0021-cli-is-the-full-surface-layer-1-churns.md) decision 6 point 3 scopes enumeration to exactly those. As built, an agent that drafts a lexeme-form removal and changes its mind cannot drop that operation and must rebuild the draft, which defeats the requirement decision 6 exists to serve ("remove specific items piece by piece"). The one real hazard — removing a delete so that a sibling `create` overwrites an incumbent instead, destroying it (`DisplacedOccupantFactTests`) — is already prohibited by `AGENTS.md` rule 5, which forbids two operations addressing the same slot in a finalized Proposal. **Recommendation: drop the refusal, keep warn-and-force, and record that decision 6 point 4 has no live instances** so the next implementer does not invent one either. Worth noting as a delegation lesson: a rule with no instances invites a subagent to manufacture one. | open — owner's call |
| B26 | **Ingested text cannot yet reach a coverage figure — the tokenisation step does not exist.** `add-corpus` / `add-document` / `add-corpus-bundle` store a `StoredCorpus` of running text, and `CoverageFigure.Compute` consumes a `CorpusDescriptor` of distinct word forms. **Nothing connects them.** The only producer of a descriptor is `LcmWordformCorpus.Extract`, which reads the open project's own wordforms — so as built, a 100 MB eBible pull can be stored, hashed and licence-checked, and then measured against nothing. This is the single largest gap in the corpus work and it is a build task, not a decision: the missing piece is a tokeniser that turns Documents into forms and records what it did in the `TokenisationRecord` the corpus already carries. SIL.Machine's `LatinWordTokenizer` is the house candidate ([ADR 0037](adr/0037-fetching-lives-outside-motif.md)); the bundle format already names a method and version, so the record has somewhere to go. Note the deliberate asymmetry the bridge must preserve: a Document keeps order and repetition, a descriptor is sorted and distinct, and only that direction is derivable. | **fixed 2026-08-09** — `CorpusTokenisation.ToDescriptor` bridges the two behind an `IWordTokeniser` seam, with a BCL-only `WhitespaceAndPunctuationTokeniser` and **no SIL.Machine dependency**, since nothing in Motif references it and adopting it is a decision in its own right. The declared tokenisation is binding: a tokeniser whose name or version disagrees with the corpus's `TokenisationRecord` is refused rather than silently re-stamped. Provenance flows through, so an unattested corpus still cannot support an accuracy figure after the bridge. See `B29` for what the shipped tokeniser gets wrong. |
| B27 | **"Unknown licence blocks derivation" is my decision, not the owner's.** `LicenceCapabilities` makes every permission three-state and treats *nobody established it* exactly as *forbidden* for derived artefacts, with a different explanation. I built it that way because it is the safe direction and cheap to reverse, but the question was asked and never answered, so it should not read as settled. The live consequence: a corpus ingested with no licence flags — which is what happens by default — cannot feed an n-gram model or a published word list until somebody records a basis. Reach and coverage figures are unaffected. If the answer is that unknown should permit derivation, the change is one property on `LicenceCapabilities` and the tests named for it. | **fixed 2026-08-09 — ratified by the owner.** Unknown blocks derivation. No code change: the behaviour shipped this way and is now a decision rather than my default. |
| B28 | **A Corpus cannot be replaced once ingested, and Motif does not track whether its source has moved.** `AddCorpus` refuses an existing id, because replacing it would orphan every figure computed over the old contents. | **wontfix (recorded) 2026-08-09 — by decision, not by omission.** The owner's ruling: *"Don't track eBible — we grab data when we want to. Add in no detection or checking or polling."* So there is no source-watching machinery of any kind: no polling, no revisiting a URL, no staleness nagging. A corpus is what was fetched, and it stays that until a person fetches again — at which point a new corpus id is the whole mechanism. The per-document SHA-256 stays, but as **identity rather than surveillance**: it says what a figure was computed over so two corpora can be compared, and it is never pointed back at the source. This closes the question rather than deferring it; reopen only if somebody actually wants supersession, and note that an earlier draft of this row proposed retaining every pull's hashes, which would have been the first authored thing in a store [ADR 0036](adr/0036-motif-has-its-own-data-store.md) says holds only derived data. |
| B29 | **The shipped tokeniser strips an edge glottal stop written as a plain or curly apostrophe, and that hits exactly the languages this project serves.** Measured 2026-08-09: .NET classifies `U+0027` APOSTROPHE and `U+2019` RIGHT SINGLE QUOTATION MARK as **punctuation**, so `WhitespaceAndPunctuationTokeniser` strips them at token edges; `U+02BB` ʻokina, `U+02BC` MODIFIER LETTER APOSTROPHE and `U+0294` LATIN LETTER GLOTTAL STOP are **letters** and survive. A project using the typographically correct characters is fine; a project using what a keyboard produces — most legacy data — silently loses a word-initial or word-final glottal, and the altered form fails to parse and reads as a grammar gap. **This is the same failure the word-internal rule exists to prevent, at the other end of the token.** It is not fixable by character classification alone: `'mbali'` is genuinely ambiguous between a quoted word and a word with edge glottals, and nothing in the character stream distinguishes them. The real fix is a writing-system-aware tokeniser consulting the project's Valid Characters list, which Motif already reads in `WritingSystemInventory`. Pinned by `EdgeGlottalsSurviveOnlyWhenWrittenWithLetterCharacters` so it stays a decision rather than a surprise, and documented on the tokeniser itself and in its `TokenisationRecord.Notes` so it travels with any figure computed from it. Until fixed, an affected project should declare a different tokeniser. | **researched 2026-08-09; fix decided, fallback open.** FieldWorks' real segmenter is `WordMaker` (`liblcm/src/SIL.LCModel/DomainServices/ITextUtils.cs:1573-1584`) and it does **not** use `char.IsPunctuation` — it calls per-writing-system `get_IsWordForming`, which checks the project's LDML `main` character set before falling back to ICU letter categories (`CoreWritingSystemDefinition.cs:166-172`). **So the fix is to read the writing system**: `ValidCharacters.IsWordForming(char)` is public (`ValidCharacters.cs:235`) and Motif already loads `ValidCharacters` in `WritingSystemInventory.cs:127` for a count, so this costs no new dependency. **And FieldWorks has the identical behaviour when a project has never populated its character sets** — `WritingSystemManagerTests.cs:219-233` asserts `get_IsWordForming(''')` is true with `'` in `main` and false after `CharacterSets.Clear()`. That reframes this row: not a Motif defect but a shared characteristic with a per-project remedy, and populating Valid Characters fixes both tools at once. FieldWorks' designers did intend better — `DefaultWordformingChars` (`ValidCharacters.cs:50-67`) is `{ ' , - , ZWNJ, ZWJ, superscript 0-9, Tibetan tsheg x2, saltillo x2 }` — but it is private and applies only when the wizard classifies a newly-seen character. **Still open: what to do for an unconfigured writing system.** SIL.Machine is not the answer — `LatinWordTokenizer` folds a leading apostrophe *into* the word (`LatinWordTokenizer.cs:93`), is hardcoded Latin-script, and has no writing-system awareness; worth adopting later for Latin edge cases (netstandard2.0, NuGet `SIL.Machine` 3.9.0, so net48-safe for scope 2). ICU `BreakIterator` is a dead end: FieldWorks does not use it for word segmentation either. **Fixed 2026-08-09** by `WritingSystemWordTokeniser`, which segments as FieldWorks' `WordMaker` does — maximal runs of codepoints `get_IsWordForming` accepts, surrogate pairs kept whole. It is a **new** tokeniser (`fieldworks-word-forming` v1), not a change to `whitespace-and-punctuation`, so corpora that declared the old one keep working; that is what the name/version binding in `CorpusTokenisation` is for. Unconfigured writing systems get FieldWorks' behaviour exactly, apostrophe dropped, **by decision** — everything Motif measures is measured against a grammar whose lexicon was built with FieldWorks' segmentation, so being independently smarter would manufacture false gaps. `WhyTokenisationMayBeDegraded()` reports the undeclared case and names the only real fix: running the Valid Characters wizard, which repairs FieldWorks and Motif at once. **Found in review, and larger than the apostrophe problem that started this row:** `TsStringUtils.IsWordForming` admits only letter, mark and modifier-symbol categories — **no numeric category at all** — so this tokeniser *splits* on digits rather than trimming them. `2nd` yields `nd`, and an orthography **marking tone with digits** is shredded (`ma1` yields `ma`, `a1b` yields two tokens). That is FieldWorks' behaviour too, with the same remedy — declare the digits in `main` — and it means the two tokenisers disagree about much more than punctuation. Pinned by `DigitsSplitAWordUntilTheWritingSystemDeclaresThem`. |
| B30 | **RESOLVED.** A target the Proposal itself mints is excluded from the footprint on both sides of the drift check, because it has no prior state to have drifted from: its "before" is that it did not exist, and nothing outside the Proposal can touch an object the Proposal is the one bringing into existence. The rule lives in `FootprintPlan` and both `ProposalDryRunner` and `FootprintProbe` call it, so the two computations cannot disagree about what was measured — which was the real hazard, not the throw. Pinned by `FootprintPlanAgreementTests`, which asserts the Dry Run digest equals the Apply pre-flight digest across five shapes, and by `TheChainedShape_TargetingAFormThisProposalMints_AppliesAndSaves`. Writing those tests also surfaced that two lexeme-form creates on one entry address a single owning/atomic slot, which design rule 5 already forbids. The original finding: Apply's pre-flight read every operation's live footprint before any operation ran, so an operation targeting an `entityId` an earlier operation minted threw, while Dry Run — sequential, one transaction — did not. Such a Proposal dry-ran cleanly and then failed at apply. | fixed |
| B31 | **A malformed payload closed the connection instead of refusing it.** `WorkerControlConnection.RunAsync` deserialized and dispatched with no guard, and `WorkerServer.HandleConnectionAsync` wrapped both the handshake *and* the whole command loop in one bare `catch` whose comment described only the handshake. An unknown `Command`, or a known command carrying a semantically invalid field (a relative path failing `ProjectLocator.RequireFullWindowsPath` inside `JsonSerializer.Deserialize`), threw past every handler and tore the connection down without writing a response frame — the client saw a pipe close, never the typed failure the handlers could already return. Contract tests proved the *records* reject bad input but nothing drove a malformed payload through a live connection, so the effect went untested. **Decision (owner, 2026-08-25): typed refusal, keep the connection.** The worker now answers `WorkerRefusalEnvelope` — a request id, a closed `WorkerRefusalReason` (`UnknownCommand`, `CapabilityNotNegotiated`, `ProtocolMismatch`, `MalformedPayload`), and the negotiated protocol — and goes on serving; the client raises `WorkerRequestRefusedException` on that one request and leaves every other outstanding request alone. Two boundaries are deliberate. **A frame carrying no usable request id is still fatal**, because a refusal nobody can correlate is indistinguishable from silence — framing violations and pre-handshake traffic keep closing the connection. **The refusal carries no caller text**, only the closed reason, so it can never reflect an unbounded payload or a filesystem path back onto the wire. Handler failures that are not payload-shaped (`InvalidOperationException` from an unready runtime, transport and cancellation failures) still end the connection, which is unchanged and is the next thing to revisit if it proves too blunt. Fixing it also exposed a real contract hole: `WorkerEnvelope` accepted a missing `Payload` and threw an opaque `InvalidOperationException` from `JsonElement.Clone`, so the frame that most deserved a refusal could not get one; the envelope now rejects an undefined payload as the argument error it is. | fixed |
| B32 | **A Dry Run job now opens the Baseline twice, doubling the operation's dominant cost.** Making `dry-run` a job exposed that its prerequisite closure needs the applied-proposal set read from the state the run measures against — the Baseline-derived scratch, not the live project, or the plan is resolved against one project and measured against another. The wiring reads it with a plain `FwDataProjectLoader.LoadScratchCache` peek (`Worker/Program.cs:75`) and then opens the scratch again with `BaselineScratchFactory.OpenSingleUse` (`:80`). Correctness is not at risk — the published Baseline directory is immutable and neither open writes to it — but a LibLCM load is the expensive part of a Dry Run, measured at ~10s on a 43 MB project, so this is ~10s to ~20s there while staying invisible on the 48 KB fixture the tests use. The cause is a seam, not an oversight: `DryRunScratch.ConsumeForOneRun()` is single-use and `internal` to `SIL.Motif.Runner`, so reusing one open scratch for both the applied-log read and the run needs a non-consuming accessor on that type. Worth doing before anyone runs a Dry Run on a real project; recorded rather than done because it widens a type outside the task that found it. **Fixed 2026-08-28:** `DryRunScratch` gained `PeekCache()`, a read-only accessor that never touches the `_consumed` flag `ConsumeForOneRun` checks — peeking any number of times, before or after a run, changes nothing about whether that flag lets a run through, so single-use is exactly as strict as before. `DryRunJobHandler`'s two independent hooks became one `openScratch` (opens the one scratch and peeks it for the applied-proposal log) plus a `runDryRun` that now consumes that same scratch instead of reopening the path; the handler disposes it once the run returns, in a `finally` so an exception between the two no longer leaks the open. `Worker/Program.cs` and the CLI's job-running test fixture both moved to the one-open shape. `ADryRunJobOpensThePublishedBaselineExactlyOnce` and a strengthened `TwentyDryRunsFromOneCaptureAreIdenticalAndLeaveTheBaselineByteForByteUnchanged` (now asserting 20 scratch opens for 20 jobs, not 40) count real `LoadScratchCache` calls through a loader subclass and prove exactly one open per job; `DryRunScratch_PeekCacheDoesNotConsumeAndASubsequentRunStillSucceedsExactlyOnce` pins that a peek never counts as the run. No other caller in `src` was found opening a published Baseline more than once. | fixed |
| B33 | **Moving Drafts into the database removed the only way to abandon one, and the refusal still named it.** A Draft is now a `Proposals` row and no verb removes one: the CLI dispatches 31 verbs and none is `discard`, `delete` or `drop`, while `ProposalRepository.DeleteArchived` refuses anything non-terminal and a Draft is not terminal. Under the file store the escape hatch was outside the CLI — you deleted `drafts/<name>.json` by hand — so the surface never carried the verb and losing the hatch went unnoticed. Two collision messages meanwhile told the caller to "Finalize or delete it" and "Finalize or discard it", naming an action that does not exist, and they had drifted apart because the check was inlined at three sites. The standards axis of a code review reported the duplication; the dead-end instruction was found while verifying that report. **Messages corrected** to name only what a caller can do, and the transactional check extracted to one helper, pinned by an assertion that the refusal does not say "delete it". **The capability is still missing:** a caller who wants to abandon a half-written Draft has no way to, and `discard-draft` is the obvious verb. **Closed 2026-08-28:** `discard-draft --project <fwdata> --draft <name>` deletes a never-finalized Draft's row and frees its name immediately. A Draft `reopen` produced keeps its source Proposal's committed `ProposalRevisions`/`Decisions` behind it, so discarding one instead clears only `DraftName`/`DraftJson` — the exact inverse of what `reopen` set — leaving that Proposal exactly at its prior committed revision, name free for reuse either way. | fixed |
| B34 | **An operation with no after-state renders as one whose after-state is empty.** `ProposalDetailProjectionBuilder.cs:20` maps `AfterJson: op.After?.GetRawText() ?? "{}"` into `ProposalOperationView.AfterJson`, which is a non-nullable `string` on the `show` and `--json` surface. So an operation that carries no after-state at all is indistinguishable from one whose after-state is a genuinely empty JSON object. Same shape as the absent-digest sentinel fixed alongside it, and unlike that one this path **is** reachable: `show` renders operations for any committed Proposal. Found by the agent fixing the digest and reported rather than fixed, because widening a second Contract member is a scope decision. A lower-confidence sibling worth checking at the same time: `Commands.cs:1427` flattens an absent Label with `manifest.Label ?? ""` for the applied-log description, and whether that description is ever exposed distinguishably was not traced. **Fixed 2026-08-28:** `ProposalOperationView.AfterJson` is now `string?`; the builder no longer substitutes `"{}"`, and both renderers omit the `after` figure entirely when it is absent, matching how the digest fix (B33's neighbor, commit 798aebd) carries `CurrentIntentDigest`'s absence through. A committed Proposal whose operations all carry an after-state renders byte-identically to before, pinned by `ProjectionRenderingTests.ProposalDetail_CommittedProposal_TextAndJsonArePinned`. **The Label sibling was traced and is not the same bug:** `manifest.Label ?? ""` feeds `ProposalApplier.Apply`'s `description` parameter, which is `string` (not nullable) and documented as defaulting to empty by design; the applied-log line format (`AppliedLogFormat`) stores `Description` as a plain delimited text field with no representation for "absent" distinct from empty, and `AppliedLogEntryView.Description` carries the same non-nullable shape through to `log`'s text and JSON. There is no nullable member anywhere on this path for an absence to be lost from, so left unchanged. | fixed |
| B35 | **`PanGlossAssessor.ProduceAsync` can invoke the parser three times for one scope, same shape as B32.** A scope collecting `ParseTime`, `Correctness` and `ObjectTiming` together drives the assess pass (for the grammar hash and Correctness's raw material), the plain batch pass (for ParseTime), and the stats-collecting batch pass (for `ObjectTiming`) as three separate process invocations, where PanGloss itself is capable of yielding several kinds from one run. The assess pass is now unconditional — every produced kind must cite `AssessReport.GrammarSourceSha256`, and only that pass carries it — so even a scope collecting `ParseTime` alone pays for it. Nothing in `IAssessor` forces this shape: it is contained entirely inside `PanGlossAssessor`, so collapsing the three calls later needs no interface change, and reusing one assess pass's `BatchAnalysis`-shaped timing (once the stats-cache read-back exists) may make the collapse natural rather than a rewrite. Recorded rather than fixed because doing so now would mean guessing at a CLI surface (`pangloss batch --stats --cache`, `pangloss stats --format jsonl`) that has not yet been exercised against the real binary.  **A second, smaller cost was added to this row while fixing the seam's raw-form hole:** the assess pass now runs unconditionally, so a scope wanting only `ParseTime` pays for it too. That is not an oversight — every `ProducedAssessment` cites `GrammarSourceSha256`, which is *the parser's* hash of what the parser actually read (`provenance.sourceSha256`), and `GrammarCoverageFigure` records the standing rule that Motif never hashes a grammar itself because that would identify what Motif thinks was parsed rather than what was. Hashing the file locally would be cheaper and wrong. The fix, if one exists, is a cheaper PanGloss route to provenance without a full assess pass — a question for PanGloss, not something to guess at here. | open |
| B36 | **A Proposal's Trials and Dry Runs are not swept when it is applied, because `Jobs` cannot be asked which Proposal a job belonged to.** ADR 0042's third amendment says a Proposal's Trials, Dry Runs and Assessments become deletable on apply, by configuration. Assessments are swept — `DeleteByProposal` takes the promoted id as a SQL parameter to spare. The job rows are not: `Jobs` carries no `ProposalId` column, which is the same absence `motif jobs assessments` exists to work around by reading ids out of a job's `ResultJson`. Sweeping them properly needs a schema migration and a real reverse lookup, not a scan of JSON. Left undone deliberately rather than bolted on. | open |
| B37 | **`apply` enforces no Proposal status, and the lifecycle document claimed it did.** There is no `ApplicableFrom` guard beside the `DeferrableFrom`/`ApprovableFrom`/`RejectableFrom`/`SupersedableFrom` arrays, and `ProposalWorkflowTests` applies straight from `proposed` without calling `approve`. `docs/proposal-lifecycle.md` stated *"Only approved may be applied"* under a header promising the guards were read from the code — so a claim that was never checked travelled under the authority of ones that were. The document is corrected. The behaviour is not, because it is a real question rather than an oversight: applying is the only irreversible step, which argues for a guard, while an AI loop applying its own proposals argues against one. Found while reviewing the promotion-and-sweep work, when an agent disclosed that overwriting a Decision on apply costs nothing today *because* apply never requires one.  **Resolved 2026-08-31 by removing the question.** The owner ruled that there is no approval: `approve`, the `approved` status and the whole Decision record are gone, and `apply` refuses on evidence instead — no Assessment for the current content, an Assessment measured against a different project state, or a regression — with `--force` overriding all three. The regression gate is on by default now rather than opt-in. See the 2026-08-31 section of `proposal-lifecycle.md` and the amendment to ADR 0031. | fixed |
| B38 | **An `ObjectTiming` Assessment cannot be stored, because `Assessments` has no column for a cache path or digest.** ADR 0042 decision 8 says Motif reads PanGloss's stats cache and *the database records its path and digest*, per ADR 0041 decision 9. `PanGlossAssessor` computes both and returns them as `AssessmentRaw.FileCache`; nothing persists them, and `TrialJobHandler.WordsFor` throws `NotSupportedException` naming the reason rather than dropping them silently — which is the right refusal but leaves the kind undeliverable end to end while sitting in the closed `AssessmentKind` enum. Schema generation 10 added Assessor, Kind, scope, tokeniser and Baseline token and did not add these two. Found by the spec axis of a code review, which believed it was already recorded as B35; it was not — B35 is the three-invocation cost, and a grep for a cache path in this file returned nothing. Recording it now so the next person to want per-rule timing finds it here rather than in a throw. **Fixed 2026-08-30:** schema generation 11 (alongside B39's rename, in one migration) adds `Assessments.CachePath`/`CacheDigest`, both nullable since only an `ObjectTiming` row carries them. `TrialJobHandler` no longer refuses the kind: `MaterialFor` reduces `AssessmentRaw.FileCache` to an empty word list plus the cache path and digest, and `NewAssessmentRecord`/`AssessmentRecord` carry them through to the store. Round-trip pinned by `AnObjectTimingAssessmentRecordsTheCachePathAndDigestWithNoWordRows`. | fixed |
| B39 | **A resolved Selection is stored as a Corpus, which the glossary explicitly warns against in both directions.** `CONTEXT.md` defines a **Selection** as "a named set of word forms... listed out in full" and lists **corpus descriptor** under its own _Avoid_; it defines a **Corpus** as "a body of running text" and lists **word list** under _Avoid_. `TrialJobHandler` resolves a scope's query into a Selection and then stores it with `CorpusDescriptor.Create(...)`, into `Assessments`' `CorpusId`/`CorpusWordsJson`/`CorpusSha256` columns. **This is inherited, not introduced**: those columns and `CorpusDescriptor` both predate the assessment loop — the descriptor arrives in `b1ac754` — so the loop perpetuated a naming the glossary later contradicted rather than creating it. That is why it is recorded rather than fixed here: 44 usages across 15 files, several of them genuinely corpus-shaped (`CorpusDocument`, `CorpusTokenisation`, `IWordTokeniser`), plus the schema columns. Worth deciding deliberately — rename the concept in code and migrate the columns, or amend the glossary to admit that a Corpus descriptor is how a Selection is carried. **Fixed 2026-08-30 — a rename, not a decision:** every usage of `CorpusDescriptor` did carry a Selection, in every call site checked, so it becomes `Selection` (file `Corpus/Selection.cs`); its `CorpusId` field, a caller-assigned label rather than an identifier, becomes `Name`. `CorpusTokenisation.ToDescriptor` becomes `ToSelection`; `LcmWordformCorpus.Extract` becomes `ExtractSelection`. The rename reached every downstream field that carried the same identity under a `Corpus*` name: `GrammarCoverageFigure.CorpusId`/`CorpusSha256` → `SelectionName`/`SelectionSha256`, `ReportableAssessment`, `AnalysisAssessmentProvenance`, and the `Corpus` field on `NewAssessmentRecord`/`AssessmentRecord`/`StoredAssessment`/`CorrectnessAssessment` → `Selection`. `StoredCorpus`, `CorpusDocument`, `CorpusTokenisation`, `CorpusProvenance`, `IWordTokeniser`, the corpus CLI verbs and the `Corpora`/`CorpusDocuments` tables are untouched — those are genuinely about a Corpus. `Assessments`' `CorpusId`/`CorpusWordsJson`/`CorpusSha256`/`CorpusProvenanceJson` columns are renamed `SelectionName`/`SelectionWordsJson`/`SelectionSha256`/`SelectionProvenanceJson` in schema generation 11, the same generation B38 adds its two columns in. No JSON contract or wire shape changed: the rename stayed inside `SIL.Motif.Host`/`SIL.Motif.Worker`, and the one JSON key that echoed the old name (`report`'s stored `EvidenceJson.corpusSha256`) keeps its existing key, now fed from the renamed field. Behaviour unchanged throughout — the existing test suite was adapted to the new names, not rewritten. | fixed |
| B40 | **`SqliteAssessmentStore` can read the real `Assessments` table but can never write it, and a second DDL now has to be kept in step with the schema by hand.** Generation 11 gives `Assessments` seven `NOT NULL` columns (`Assessor`, `Kind`, `ScopeJson`, `ScopeDigest`, `TokeniserName`, `TokeniserVersion`, `BaselineToken`) that the store's `INSERT` does not supply, so `Save` fails against any migrated database with `NOT NULL constraint failed: Assessments.Assessor`. Production never notices, because it reads through this store (`Commands.cs:202`) and writes through `AssessmentRepository`. `Save` therefore only ever wrote to the table `EnsureLegacyTables` fabricates on every plain connection, which is why the column-name break below went unseen. The immediate fix split that DDL so the fabricated shape carries generation 11's column names (`MotifSchema.StoreOnlyDdl`), leaving two definitions of one table that must not drift. **Decision needed:** delete `Save` and stop fabricating, so a store is always handed a migrated database, or keep two writers and pin the shapes against each other. | ADR 0041 decision 1 says the database is the only store; it does not say which component owns this table.  **Fixed 2026-08-30.** The table has one definition again. `IAssessmentStore` (one implementation, no consumer in `src/`) and `SqliteAssessmentStore.Save` are deleted, so the store is what production already used it as — the read side, with the worker writing through `AssessmentRepository`. `EnsureLegacyTables` now fabricates only the Corpus tables, so an Assessments table exists because a migration made it and never otherwise, and `StoreOnlyDdl` is gone. Every test seeds through the repository, which is how the drift went unseen. `AssessmentIdentity` went with `Save`; see B43. | fixed |
| B41 | **The CLI still asks for `--current-corpus-sha256` when what it wants is a Selection's digest.** `Program.cs:51` reads the flag, and `currentCorpusSha256` runs through `Commands.Analyses`, `AnalysisAggregateProjectionQuery` and `GrammarCoverageFigure.IsCurrent`. The report *text* around it was corrected to say selection, so the flag now disagrees with the output it feeds. Renaming it changes a published CLI surface, which ADR 0040 makes the one API. **Decision needed:** rename with no alias (hard cut, consistent with ADR 0041's no-migration stance), rename with the old flag accepted for a while, or keep the flag and accept the mismatch. | `CONTEXT.md` Selection/Corpus; ADR 0040 decision 1.  **Fixed 2026-08-30.** Hard cut, no alias, following ADR 0041's precedent for `--store`: the flag is `--current-selection-sha256`, the JSON key is `selectionSha256`, and the report text says selection. README and usage banners updated. | fixed |
| B42 | **`motif analyses --assessment` rejected every id the worker mints.** `Program.cs:58` and `Commands.Analyses` both required `Sha256Value.IsCanonical(assessmentId)`, but `TrialJobHandler` mints `CanonicalId.Mint("assessment/")`, so the verb could only ever accept ids produced by the dead `SqliteAssessmentStore.Save`. Found by making a CLI test seed the way production does. **Fixed 2026-08-30.** Both sites validate with `CanonicalId.TryParse`. | The same root cause as B40: the read path was only ever exercised with the dead writer's output. | fixed |
| B43 | **Two identity schemes and two pruning mechanisms, one of each now unused.** `AssessmentIdentity` derived an Assessment's id from its content and `IAssessmentStore` documented that as the rule ("Identity is content, not a caller-supplied name"); the worker mints a time-ordered `CanonicalId` instead, so the documented rule was never true of a real Assessment. Deleting the dead writer removed the contradiction but also removed content-addressing as an option without anyone choosing. Separately, `Pin`/`Unpin`/`ListUnpinnedAssessmentIds` are a refcounting mechanism nothing calls, deliberately built as "mechanism without policy" against ADR 0036's open pruning question — but ADR 0042 decision 6 and `proposal-lifecycle.md` have since settled pruning as a sweep scoped to the applied Proposal, which `IAssessmentRepository.DeleteByProposal(proposalId, exceptAssessmentId)` already implements. **Pruning resolved 2026-08-30:** the sweep won, because it is the one that runs — `Commands.cs:1513` calls `DeleteByProposal` when `PurgeOnApply` is configured, so the policy ADR 0042 decision 6 describes is already live and `Pin`/`Unpin`/`ListUnpinnedAssessmentIds` were a second mechanism for a settled question. Deleted, along with `Exists` and `List`, which had no caller left anywhere. The `AssessmentPins` table stays until some generation has another reason to move; dropping it alone is churn. **Identity settled 2026-08-31:** minted, time-ordered ids stay. An Assessment records a measurement event — when it ran, which Trial, against which Baseline — and two identical runs a week apart are legitimately different rows. The old content hash included `OutcomeDigest`, an output, so it could never have skipped work anyway; run-skipping, if wanted, is a cache keyed on inputs. | ADR 0036 open question; ADR 0042 decision 6. Blocks B36. | fixed |
| B44 | **`SqliteMotifDatabase.OpenConnection` opens a project database without the schema gate.** `MotifSqliteStore.Open` now refuses a database whose stamped schema is not this build's, but `SqliteCorpusStore` and `SqliteAssessmentStore` go through a second opener that checks neither the application id nor the schema and instead calls `EnsureLegacyTables` to conjure the Corpus tables. Since the schema is now created in one step, those tables always exist in a real project database, so the fabricator's only remaining effect is to half-populate a file that was never a Motif database — the same two-openers shape as B40, one table over. **Decision needed:** route both stores through `MotifDatabase` and delete `EnsureLegacyTables`, which means deciding what a corpus verb does when no project database exists yet. | AGENTS.md rule 18; same root cause as B40.  **Fixed 2026-08-31.** One opener. `SqliteMotifDatabase`, `EnsureLegacyTables`, its race guard and `SqliteAssessmentStore` are deleted. `SqliteCorpusStore` takes the `MotifDatabase` the corpus verb was already handed by `ProjectStoreCommand.Run` and used to discard; `motif analyses` runs through the same helper and reads via `AssessmentRepository.Get(...).ToStored()`. The open question answered itself: every corpus verb already opened the gated database first, so "no database yet" was never reachable. | fixed |
| B45 | **`TrialJobHandler` writes `exception.Message` verbatim into a job's result JSON.** Its failure paths serialise `new { detail = exception.Message }` for `Failed`, `CompletedWithAssessmentFailure` and the Assessor failure case; the retired `DryRunAssessmentPipeline` recorded only `exception.GetType().Name`. Exception messages from the file system and from PanGloss carry absolute paths and, potentially, fragments of project content, and the job result is read back by `jobs show --json`. Surfaced while porting the pipeline's tests; left as-is because changing what a live handler records is its own decision. | ADR 0033 (Motif reports); the usage log is deliberately data-free and the job result should be held to the same standard. | open |
| B46 | **`ICorpusStore` has one adapter.** `SqliteCorpusStore` is the only implementation in `src/`; `CorpusIngestion`, `CorpusProjectionQuery` and `CorpusCommands.StoreFor` are written against the interface anyway. One adapter is a hypothetical seam: nothing varies across it, so callers pay the indirection for a substitution that never happens. Surfaced while giving the project database one opener. Either a second real adapter appears (an in-memory store for the tokeniser tests would be a genuine one) or the interface goes and callers take the concrete store. | Codebase-design rule: one adapter = hypothetical seam, two = real. | open |
| B47 | **A Handoff folder names no language, only a filename.** In a real package written by the production path, `grammar.json` gives `project.name` as `source` — the name of the copied file the Baseline capture produced, not the language — and the vernacular writing systems come through as bare tags. A reader given the folder and nothing else cannot say what language it is about. Found by handing a generated package to a reader with no other context, which listed "the language name and project description" first among what it needed and could not find. R5's manifest is the natural home for it. | open |
| B48 | **The package tells a reader two different things about which parser is in play.** `grammar.json` carries `activeParser: "xAmple"` while every statistics record carries `engine: "hc"`. Both are honest in isolation — the first is the project's own FieldWorks setting, the second is the engine that actually ran — but nothing in the folder says so, and a reader with no other context flagged the pair unprompted as something that could mislead. Whichever way it is resolved, a reader must be able to tell *what produced these numbers* from the numbers' own file. | open |
| B49 | **A Handoff records no word-level reason for a word that completed with no analysis.** Both words in a generated package completed, uncapped and not timed out, with zero passes. `statistics/object.jsonl` shows root-index attempts ending in `no_root`, but nothing at word level says why that word found no root, and a reader asked to explain the result named exactly this gap. R4 requires that complete, partial, absent and unavailable findings stay distinguishable; distinguishable is not the same as explicable, and the first question a linguist asks about a zero is why. | open |

## C. Upstream hazards we must defend against

Not ours to fix; the obligation is to detect, disclose, or refuse.

| # | Hazard | Defence |
| --- | --- | --- |
| C1 | **`AddCustomField` inside an open unit of work corrupts the project** (Flexicon lost 1,392 senses). | Schema ops run first in their own non-undoable unit of work; never save mid-task ([ADR 0005](adr/0005-schema-operations-non-undoable-uow.md)). |
| C2 | **Single-writer is not enforced.** A colliding writer calls `Rollback(0)` and destroys the entire open change set, indistinguishably from our own rollback. The 1-second autosave is benign; a shutdown save on a background thread is not. | Host must guarantee exclusive write access; runner treats an unexpected transaction state at task end as an external collision. |
| C15 | **The headword and homograph caches have NO bulk-invalidation hook at all** — they are incremental-only, updated solely through forward setter hooks. `ClearCachesOnUndoRedo` (which `Rollback` skips anyway) clears *only* the monomorphemic-morph cache. So a cache poisoned by an assess-rollback cannot be repaired — the only correct response is to **discard the `LcmCache` instance**, not attempt to clear it. This also means the fields that poison are wider than assumed: `MoForm.Form` and `MoForm.MorphType` do too, not just `LexEntry.LexemeForm`/`CitationForm`. | **Still true of LibLCM, and now irrelevant to Motif** (2026-08-06): the only surviving rollback is a failed apply, where the response is unconditional — discard the cache and reload, with no field list consulted. Reload is also strictly stronger than repair would have been, since it discards ADR 0005's non-undoable schema phase too. |
| C3 | **`ReferringObjects` has a first-touch whole-project cost** — `GetIncomingFields` walks to `CmObject`, force-fluffing every instance of classes carrying generic `sig="CmObject"` fields. | Host warms the incoming-reference index at load, off the interactive path. |
| C4 | **De-referencing does not cascade** — clearing a reference to an owned-collection member orphans it. | Composers emit an explicit `delete`; effects disclose the orphan. |
| C5 | **24 alpha variables maximum per rule**, and the ceiling is **per-rule**, counted by traversing `StrucDesc` then each `RightHandSides` context slot in order. Exceeding it throws and kills the whole grammar load. | Pre-apply check simulating that exact traversal. |
| C6 | **MPR referential integrity is unguarded** — ~16 raw dictionary indexers mean any dangling inflection-class / prod-restrict / inflType reference throws and kills the load. Happens in the wild: `GenerateHCConfig` crashes on the Amharic project via a stale `MoMorphAdhocProhib`. | Pre-apply referential-integrity validation. |
| C7 | **An invalid environment string becomes "applies everywhere"** — invalid data silently becomes *more* permissive. | Validate against the `PhonEnvRecognizer` grammar and surface the widening. |
| C8 | **Silent-loss surface**: one bad phoneme makes a whole natural class permanently unusable; one unloadable form drops an entire adhoc prohibition; entries/rules/templates vanish when they end up empty; a stem name with no regions vanishes; natural-class abbreviations collide with last-one-wins. | Predict and report each as part of assessment. |
| C9 | **`PhSegmentRule.InitialStratum`/`FinalStratum` are silently ignored** despite the model comment promising per-rule stratum scoping. `MoStratum` is read nowhere. | Not offered as controls. |
| C10 | **`MoStemName.DefaultAffix`/`DefaultStem` unread** — only `Regions` drives suppletion, so a fallback form is inexpressible. | Documented as unreachable. |
| C11 | **`MoGlossItem`'s entire 10-field gloss system is never consulted** — the gloss HCLoader uses comes from `LexSense.Gloss`. | Documented; don't offer the dead path. |
| C12 | **`InvalidRewriteRule` is declared but never invoked**; `ConsoleLogger` throws `NotImplementedException` on `UnmatchedReduplicationIndexedClass`, so a bad reduplication index crashes the CLI exporter while loading fine in FLEx. | Don't rely on the logger surface for completeness. |
| C13 | **On-disk save is not atomic** — temp file then two separate `File.Move`s. | Host owns save/backup. |
| C14 | **Realizational morphology is unreachable** — `HCLoader` carries `// TODO: use realizational affix process rules`, so paradigmatic blocking and `LexFamily` suppletion cannot be expressed however we write LibLCM. | Documented ceiling; never promised. |
| C16 | **The custom-ICU fallback silently changes normalization.** LibLCM P/Invokes `SilIcuInit` in a SIL-built ICU (`liblcm` `CustomIcu.cs:32,260`) carrying FieldWorks' `nfc_fw`/`nfkc_fw` tables. When that library is absent it does **not** fail: `DllNotFoundException` is caught, `HaveCustomIcuLibrary` goes false, and normalization silently degrades to stock `nfc`/`nfkc` (`:224-247,409-419`). The on-disk `.fwdata` form is **NFSC** (`TsStringSerializer.cs:38-39`, `Kernel.cs:2408-2411`), so normalization *is* string identity. A device without `icu-fw` will disagree with a desktop about whether two strings are the same string — no exception, no log, across a sync boundary. | Any platform target must ship the custom ICU, or the divergence must be made loud. Applies to **any** system on this data, not only this one. |
| C17 | **Distributed collaboration on a grammar is not achievable, and no upstream fix would change that.** The data-model obstacles are real — no change history in `.fwdata`, positional sequence order, forward-only derived-value hooks, per-project custom-field ids — but they are not the blocker. Grammar rules are **interdependent**: order encodes interaction, so two locally-correct edits can compose into a grammar neither author intended. That is a property of grammars, not of FieldWorks, so fixing all four upstream would not unlock it. Measured 2026-08-06: hand-authored grammar is ~220 objects of 152,222 in the larger project (~0.15%), with zero phonological and zero compound rules in either sampled project — small enough that one integrator is cheaper than any reconciliation scheme. Accepted permanently; see [ADR 0031](adr/0031-collaboration-follows-the-data-not-the-surface.md). | wontfix (by nature) |

## D. Our own tooling limitations

| # | Issue | Status |
| --- | --- | --- |
| D1 | **The HCLoader extractor matches field *names*, not `class.field`.** The never-referenced set (321, post-D5) is therefore precise, but the referenced set (152) is a conservative over-approximation. Per-class precision needs the curated map. | wontfix (recorded) |
| D2 | **Method-mediated reads are invisible to the extractor.** `PhMetathesisRule.LeftSwitchIndex`/`RightSwitchIndex` are read via `GetStrucChangeIndices()`; patched by hand. Others may exist. | open |
| D5 | **Scope over-inclusion: `TextTag`.** It entered the in-scope set only because `StText`/`StPara` were pulled in as rich-text containers, and `TextTag.BeginSegment`/`EndSegment` target the out-of-scope `Segment` (interlinear) class with no authoring path — structurally the same failure as the derivation-trace false positive. 5 rows demoted; in-scope is now **473 props / 95 classes**. The generation rule needs `StText`/`StPara` treated as **terminal** containers (walk *to* them, never *through* them). | fixed |
| D6 | **Inventory generation is not yet a committed script.** `classify.ps1` is reproducible but the inventory itself was produced by ad-hoc commands, so the D5 fix was applied surgically rather than by regeneration. Needs a `manifest/generate-inventory.ps1` before the next LibLCM bump, or the drift gate cannot actually be re-run. | open |
| D7 | **`classify.ps1` no longer reproduces the checked-in inventory, and nothing notices.** `manifest/README.md` says to rerun it rather than hand-edit the TSV, but running it (2026-08-06) rewrote **26 rows** — every `CmAgent`, `TextTag`, `WfiAnalysis`, `WfiGloss`, `WfiMorphBundle` and `WfiWordform` row — reverting the `Group`/`HcReachable`/`Rationale` values ADR 0025's analysis-approval scoping had set by hand. The script does not know about ADR 0025, so it is now a *partial* producer masquerading as the whole one, and the next person who follows the README loses that work silently. Found while retiring the `AssessPoisonsCache` column, by diffing before and after rather than trusting the rerun. The `.g.cs` files have a drift guard (`GeneratedFilesAreUpToDateTests`) for exactly this failure; the manifest has none. Fix is either to teach the script the ADR 0025 rows or to demote it in the README from producer to *first-pass* tool — with a test that pins whichever answer is chosen. Related to [D6](#). | **guard built 2026-08-07** — `ManifestHandAuthoredRowsTests` pins the 21 hand-authored ADR 0025 rows, so a rerun that clobbers them now fails loudly instead of silently. The divergence itself remains: the script still does not reproduce the file, and the README demotes it to a first-pass tool. Open until the script is either taught the ADR 0025 rows or formally retired as a producer. |
| D3 | **A regex bug silently dropped every relation accessor** in the first extraction run (optional-suffix group let the greedy capture swallow `...OC`/`...RA`), reporting 32 distinct fields instead of 105. Fixed; recorded because the failure mode was silent and plausible-looking. | fixed |
| D4 | **Scope by naming heuristic was wrong in both directions** — admitted the derivation-trace family, excluded `CmPossibility`/`CmMedia`/`CmResource`. Replaced by computed owning-edge reachability. | fixed |
| D8 | **The description check verifies form, not truth — measured 8% wrong in the first large batch.** 78 agent-written kind descriptions all passed `DescriptionCheck`; 7 were wrong and 4 inverted the field's meaning. All five exception-class fields (`ProdRestrict` and its `From`/`To` variants) said an exception feature *blocks* an affix, where `MasterLCModel.xml` states the stem **must bear** the class for the affix to attach (English `-ity` only on `[+Latinate]` stems). Two more invented rule-interaction rationale for fields the model calls plain positive and negative rule-feature requirements. **Polarity is the failure mode**: a condition stated backwards reads perfectly and no mechanical check can see it. Fix available and cheap — 60 of the 92 described kinds (65%) have a substantive model comment, median 398 characters, in the file the generator already parses, so seed from the comment and record which comment it came from; the remaining 35% are unsourced claims and `Reviewed=draft` should mean exactly that. See [ADR 0023](adr/0023-derived-kind-names-required-descriptions.md) as amended. **Fixed 2026-08-10.** Nobody wrote a sentence to close it. `refresh-descriptions` copies and cites; of 93 rows, 66 now come from a `MasterLCModel.xml` comment, 7 from FieldWorks' `ContextHelp.xml`, 9 from the field's own page inside `FieldWorks_Language_Explorer_Help.chm` (opened with `hh.exe -decompile` — an earlier pass reported that file as unextractable, which was wrong), and 5 are the hand-corrected `ProdRestrict` family, preserved verbatim with a citation attached. 2 carry the new `no-source-exists` value, which cites the search rather than a source and, for `FsFeatureSpecification.Feature`, re-derives the structural reason on every build. 2 more are `adapted`: the two `PhSimpleContext*.FeatureStructure` rows are derived from their already-cited `PhSimpleContextSeg` sibling by a checked-in substitution rule, licensed by the abstract parent's own comment that the subclasses "define a featureStructure attr... differently for each class". The substitution is re-applied to the sibling's current text on every refresh and **fails** if the clause it replaces is no longer there, so an adapted row cannot outlive the sentence it was adapted from — which a copied-and-edited sentence would have done silently. **2 remain genuinely unsourced** and are listed by name in the refresh output: `MoInflAffMsa.PartOfSpeech` and `MoUnclassifiedAffixMsa.PartOfSpeech`. Provenance is now checkable rather than merely recorded: each row carries a `sha256:` digest of the fragment it was copied from — which is what lets a `hand-corrected` or `adapted` row report that its source moved, since its own prose is supposed to differ — and `manifest/source-pins.tsv` pins the three source *files* by content, so a refresh stops when one of them changes and merely reports a checkout that moved without touching them. | fixed |
| D9 | **A coverage number computed with a per-word timeout is a lower bound, and nothing currently says so.** Measured 2026-08-06: on the Amharic sample **7 of 40 words exceeded a 5-second cap**; on the mid-profile sample and Indonesian, none did. If a report counts timeouts as analysis failures it mixes "the grammar cannot analyse this" with "we stopped waiting" — a property of the machine, the thread count and the cap. That corrupts coverage as an objective function both ways: a change that only made analysis faster reads as improved coverage, and a loaded machine reads as a regression. Fix: timeouts are a third category beside ok and failed, the cap and thread count are part of a figure's provenance, and a non-zero timeout count marks the figure as a lower bound. Recorded in [ADR 0032](adr/0032-stem-assessment-is-pangloss-supplied-lexicon.md) §4; see the [timing note](research/2026-08-06-parser-timing-measured.md). **Exposure is now near nil** — the `foma` engine hit no cap on any of the three grammars, so the rule stands but is not urgent. | open (not urgent) |
| D10 | **`ClaimIoFailureReturnsPathFreeTransferInvalidWithoutClosingThePipe` races for its own file lock under full-suite load.** The test deliberately opens a transfer's `.ready` file to force an IO failure, but under a full-suite run the open itself can throw `IOException: the process cannot access the file ... because it is being used by another process` — the *setup* fails, not the behaviour under test. Observed once during a full gate, then green on an immediate re-run and green 1/1 in isolation, so it is intermittent rather than a regression. Same species as the timed-race flake already fixed in the worker event tests: the test depends on winning a race the suite does not guarantee. Not yet diagnosed — the contending holder was not identified, and MSBuild node-reuse processes lingering from earlier runs were ruled out only as a hypothesis, not by evidence. Worth pinning deterministically (retry the open, or have the production code expose the failure without needing a real lock) rather than leaving a gate that fails for reasons unrelated to the change under test. | open |
| D11 | **`ConcurrentDryRunJobsForTheSameProjectOpenOnlyOneScratchAtATime` hangs the test host when run in isolation.** Confirmed directly: the test was still running after 150 seconds with a filter selecting only it, and had to be killed. It nevertheless **passes inside the full suite**, so the gate is green and the hazard is invisible there — which is what makes it worth recording. It reproduces on a commit predating the Assessment pipeline, so it is pre-existing and not caused by that work. The mechanism is unbounded waiting: the test has no timeout on the waits it depends on, so whatever ordering the full suite happens to supply is load-bearing, and without it the test simply never finishes. Two costs, both real: nobody can run a targeted filter over this area without hanging their session, and a hung `testhost` keeps a lock on the build output, so the *next* build fails with an unrelated-looking `MSB3027 could not copy` error — observed twice while working on this plan. Fix by bounding every wait in the test (`WaitAsync(TimeSpan…)`) so it fails fast instead of hanging, rather than by relying on suite ordering. Related to D10 in kind: a test whose premise depends on winning a race the suite does not guarantee. | open |
| D12 | **`MismatchedResponseIdRejectsPendingRequest` asserted an exception that races its own teardown.** The test drove a fake worker into answering with a request id nobody was waiting for, then asserted `SendAsync` threw `InvalidOperationException`. Two things happen concurrently after that frame arrives: the read loop faults with "the worker response identifier is not outstanding" and calls `Close`, which disposes the stream; and the same connection's `SendAsync` may still be inside `FlushAsync` on that stream. When the disposal wins, `SendAsync` surfaces `ObjectDisposedException` ("Cannot access a closed pipe") instead, and the exact-type assertion fails. Seen once in a full-suite run and green both in isolation and on re-run, so it was load-dependent rather than a regression -- the third racy-test finding of this kind, after D10 and D11. **Fixed rather than recorded and left:** the connection's terminal reason does not race, so the test now asserts the send failed and separately that `Completion` faulted with the `InvalidOperationException` naming the unmatched id. That is strictly stronger than what it asserted before, because it pins *why* the connection died rather than inferring it from whichever exception the racing send happened to surface. | fixed |
| D13 | **`PublishVerifiedAsync_ValidatesAnEmptySharedSettingsInAnExistingPublication` failed once under a full gate and cannot be reproduced.** Observed on one `./test.ps1` run whose only working-tree change was two added assertions in an unrelated test file. It then passed 1/1 under a targeted filter and green on an immediate clean full run, so it is load-dependent rather than a regression — the fourth of this kind after D10, D11 and D12, and like D10 it is in a test that manipulates a real publication directory. **Not diagnosed, and worse, not diagnosable from what was kept:** the failure message was lost because the gate's output was filtered to the summary lines before it was read. The mechanism is therefore a guess, and is recorded as one. Next full-gate failure in this area must capture the whole output before anything else. | open |
| D14 | **A leftover `testhost` makes the gate look like a hang, and D11 predicted it.** After a subagent's own test run, `./test.ps1` was killed at 600 seconds having produced no output, on a suite that had been finishing in 40. It was not a hang in the code: with every stray `dotnet`/`testhost` process killed, the same tree ran green in 84 seconds, and `dotnet test --blame-hang --blame-hang-timeout 90s` named no test at all. The blocked step was the *build* — D11 already records that a hung `testhost` keeps a lock on the build output and that the next build then fails or stalls. What is new is the shape of the symptom: a stalled build inside a wrapper script that pipes through `grep` shows nothing whatsoever, so it reads as an infinite test rather than a blocked compile. Cheap mitigation: `test.ps1` could refuse to start while a `testhost` from a previous run is alive, and say so. Until then, kill stray processes before concluding anything from a gate that produces no output. | **fixed** -- `test.ps1` now stops before the build when a `testhost` is already running, naming the PIDs and the command to clear them, with `-AllowRunningTestHosts` for a deliberate second run. The stall it prevents printed nothing at all, so the guard's whole value is that it turns silence into a sentence. **One interaction to know about:** the refusal names no test result, so a caller waiting on `Tests OK` or `Tests failed.` matches neither and waits forever — an agent lost an hour to exactly that, polling a file the refused gate never wrote to. The guard is not the fault there; waiting on a gate instead of running it in the foreground is, and the refusal simply gives that habit a new way to hang. **The opposite mistake is just as easy and was made three times:** killing `testhost` and starting a gate immediately leaves the build output half-written, and the incremental build that follows does not recover — producing a run that reports *fewer tests than exist* with zero failures, which reads like a pass. One such run reported 1186 of 1302 passing and printed no `Tests OK` line. Kill stray processes, then let the build settle, and treat a total that dropped as a build artefact rather than a result. |

## E. Corrections made to our own documents

Recorded because each was asserted confidently before being checked.

| # | Correction |
| --- | --- |
| E1 | Stratum guidance was written from what the code *permits* rather than what projects *contain*. Every sampled project has **zero `MoStratum` objects** and a `ParserParameters` holding only `<XAmple>` tuning. General rule adopted: coverage claims are checked against real `.fwdata`. |
| E2 | "HCLoader reads all five slot sequences" was **wrong** — an exhaustive grep finds zero references to `Slots`, `ProcliticSlots`, `EncliticSlots`. Only `PrefixSlots` and `SuffixSlots` are read. |
| E3 | The index-as-identity ordering mode was located on `PhPhonData.FeatConstraints`; it actually belongs to the **per-rule** traversal of `StrucDesc`/`RightHandSides`, so a `move` on the pool is inert. |
| E4 | "Read back, not replay" was described as consuming a LibLCM change feed; no such feed is reachable, so it is implemented as a footprint-scoped snapshot diff. |
| E5 | ADR 0005 claimed a leftover custom field is "idempotently reusable on retry"; `AddCustomField` **throws** on a duplicate name, so retry safety depends on the ensure pre-check. |
| E6 | `convert`/`replace` was framed as two different operations; it is one mechanism with two parameters (target class, target GUID), dispatching to FieldWorks' native call per construct. |
| E7 | The kind-count estimate was a 2× undercount (445 basic properties only, ignoring 453 relations). |
| E8 | **Two object counts in ADR 0031 were wrong, and the mistakes were different.** I reported zero phonological rules in both sampled projects (Amharic has **8** `PhRegularRule`) and zero compound rules in both (the larger project has **4** `MoExoCompound`, Amharic **1** `MoEndoCompound`). Cause 1: the counting script printed only the twelve largest classes per group, so I read a missing line as a zero rather than as an unprinted row. Cause 2: `MoCompoundRule` is the abstract parent — querying it returns zero however many compound rules exist. Rule adopted: **count concrete subclasses, and never let a display limit stand in for a query.** The quantitative conclusion survived (~220 → ~224 objects in the larger project); the claim "not a single rule" did not, and it had been used as evidence elsewhere. |
| E9 | **A "known limitation" recorded in ADR 0032 was withdrawn the same day.** I wrote that compounds of a supplied stem with a base stem might make overlay coverage under-report. It came from a research track for on-device delta FSTs — not the built API the ADR relies on — and even there it is named as the *experiment that distinguishes two candidate mechanisms*, with the preferred one expected to handle it. The built design states the opposite outright: the overlay recognises compound forms because morphology is unapplied before trie lookup. Rule adopted: **before recording a caveat about a dependency, confirm it applies to the component you are actually calling** — a plan document may be describing something else. |
| E10 | **A design note overstated the combinatorial-testing result it cited.** `grammar-coverage-design.md` said 2-way coverage "buys most of the value", citing the standard pairwise finding. The primary source (Kuhn, Wallace & Gallo, IEEE TSE 30(6), 2004) reports 2-way cumulative coverage of **70%–97% depending on domain**, and its own conclusion is the upper bound — no dataset needed more than 4–6 parameters — with 3–6-way named as the practical range. The authors also say the power-law shape needs far more data to generalise, and no study of a natural-language grammar's interaction space exists at all. Corrected to "a floor with a 3%–30% residual", with the order a figure was computed at now required in the report. Rule adopted: **when borrowing an empirical result as justification, read the primary source's own numbers and its own stated limits** — the popularised summary of this one is materially stronger than the paper. |
| E11 | **I described OPUS as having a Wikipedia corpus in a way that implied bulk article text; it does not.** OPUS's `Wikipedia` corpus is sentence-aligned bitext mined for machine translation, so it carries only the sentences that aligned between a language pair — not the articles. That makes it a good source for the parallel text the gloss pipeline needs and a poor one for the monolingual bulk n-gram models are built from, which wants the Wikipedia dumps directly. Rule adopted: **state what a corpus physically contains before recommending it for a purpose**, because "it has Wikipedia" and "it has Wikipedia's text" are different claims and only the second answers the question that was asked. |
| E12 | **I claimed FieldWorks has no way to record that a word form is not a real word. It has one.** `WfiWordform.SpellingStatus` is a wordform-level `undecided`/`correct`/`incorrect` flag (`Enumerations.cs:408-421`, `MasterLCModel.xml:3965-3969`) with a live Bulk Edit Wordforms column — not a `#if NOTYET` stub like the per-word context-menu commands beside it — and FieldWorks protects it: `DeleteIfSpurious` refuses to delete a wordform whose status is not `undecided` because "we know something about it that we don't want to forget" (`OverridesLing_Wfi.cs:427-436`). What survives of the original claim is narrower and was the actual point: **nothing in `ParserCore` reads it** (zero hits across `ParserWorker`, `ParseFiler`, `HCParser`, `XAmpleParser`), so a wordform marked `incorrect` is parsed and filed like any other; the flag drives the Hunspell spell-check dictionary, not analysis. Found by a subagent sent to falsify the claim after the owner pushed back on it. Rule adopted: **when asserting that a capability is absent, search for what the capability would be called by someone who was not thinking about our problem** — this was findable under "spelling", and I only looked under parsing and approval. |
| E13 | **I argued against a shared-database architecture from two concurrency claims, and both were wrong.** First I reported that Motif's 24 `BeginTransaction()` sites were `BEGIN DEFERRED` and exposed to the read-then-write `SQLITE_BUSY` upgrade trap. Microsoft.Data.Sqlite maps the parameterless overload's `IsolationLevel.Unspecified` to Serializable and issues **`BEGIN IMMEDIATE`**; a probe confirmed a second connection is refused while the first holds a transaction in which zero statements have run. Then I reported that read methods take that write lock too, making a `SELECT` block a competing writer for 30 seconds. Mapping every call site to its enclosing method disproves it: all 24 wrap writes (`Create`, `Transition`, `SaveRevision`, `Record`, `Save`, `Purge*`, `Delete*`), while `Get`, `ListAttempts`, `ListActive`, `ListAttemptsReady`, `ListArchived` are bare reads that take nothing. Both claims were asserted from a `grep` for `BeginTransaction` without reading the methods containing the hits, and the second was then "confirmed" by a probe I wrote to model the pattern rather than to exercise the code. What survived the checking was smaller and real: `SqliteCommand.CommandTimeout` defaults to 30 seconds and retries `SQLITE_BUSY` past the 15-second `busy_timeout` pragma the code sets, so the configured timeout does not govern the wait. Rule adopted: **a probe must exercise the code under discussion, not a reconstruction of it** — a synthetic reproduction can only confirm the assumption that built it. |
| E14 | **ADR 0040 claimed `SIL.Motif.Launcher` "survives intact" and that none of its parts depend on a wire. All three of its files do.** `WorkerSelector`'s own summary says so outright — *"Chooses an installed worker using wire compatibility before product ordering"* — and `InstalledWorker` carries a `ProtocolRange` and a capability list, `WorkerBuildMetadata` carries `{productVersion, min, max, capabilities}`, `WorkerLauncher.EnsureConnectedAsync` takes a `WorkerHandshakeRequest` and negotiates, and the project references `SIL.Motif.Client`. I wrote the claim from the launcher's *purpose* — catalog, newest-compatible selection, mutex, idle lifetime, none of which sounds wire-shaped — without reading its types. The mechanism does survive; the criterion does not, and the replacement is better: a job runner must be compatible with the database schema generation it will open, which is the compatibility that can actually break a user, rather than with a pipe. Recorded as a dated amendment on ADR 0040 and the implementation plan's Task 2 widened to carry it. Rule adopted: **before writing "X survives unchanged" into a decision, read X's type signatures** — a component's purpose can be independent of a thing its API is saturated with. |
| E15 | **A code review handed me a structural guarantee that does not exist, and it was the most reassuring sentence in the report.** Reviewing ADR 0041's implementation, the spec axis reported that `JobRepository.Normalize` and `ValidatePersisted` "both hard-reject `kind == \"apply\"`, so apply structurally cannot become a job". There is no `"apply"` string anywhere in `JobRepository.cs`, and `Kind` is a free string with no whitelist: an `apply` job can be enqueued today and would simply fail at dispatch with "No handler is registered". The conclusion the sentence supported was correct — `Commands.Apply` does still load the live project in-process, which I confirmed separately — which is exactly what made the invented mechanism easy to accept. Caught by grepping for the string before relaying the finding. Second instance of the same species in one session, after a probe that modelled a pattern instead of exercising it. Rule adopted: **a reported guarantee is checked by looking for the code that enforces it, not by checking whether its conclusion is true** — a true conclusion is the cheapest cover a false mechanism can have. | open |
| D15 | **`ScratchCacheEquivalenceTests.InMemoryCopy_*` fail intermittently under full-suite load, and the failure count varies between runs.** Observed twice while verifying an unrelated refactor: one run reported 1 failure, the next reported 3 — `InMemoryCopy_LosesWritingSystemDefinitions_WhichIsWhyStrategyChoiceMatters`, `InMemoryCopy_DoesNotShareStateWithTheLiveCache`, `InMemoryCopy_ProducesAUsableCacheWithTheSameObjectsAndText`. The same tree then ran green twice, and a clean tree ran green once, so the change under test was not the cause. **The varying count within identical tree states is the tell**: a deterministic break does not fail one test on one run and three on the next. Unlike D13 the test names were captured this time, and unlike D10 they are LibLCM cache-construction tests rather than filesystem ones — so the likely mechanism is contention over LibLCM initialisation under parallel load rather than a file lock, but that is a hypothesis and was not confirmed. The comparison that established it was not the refactor: full suite on a stashed clean tree, then full suite with the work restored. Worth doing that before blaming a change for any failure in this file. | open |
| D16 | **`ProjectRuntimeTests` waits one second for the thread pool ten times, and failed once under full-suite load.** `DisposalAdmissionBarrierDoesNotAllowAGetOrOpenToEscape` failed at `ProjectRuntimeTests.cs:229` — `Assert.True(entered.Wait(TimeSpan.FromSeconds(1)))`, which is a wait for `Task.Run` to be given a thread, not for anything the code under test does. The same tree then ran green three times. Distinct from D15, which is `ScratchCacheEquivalenceTests`. The file was last touched in `f77c3ca`, before the work that observed this, and nothing in that work touches `ProjectRuntime`. | A timing assumption about the thread pool, not a product defect.  **Fixed 2026-08-30.** The fourteen waits that only ever waited for the thread pool now share a 30-second `Patience`. The one bound that is the point of its test — `TryReleaseIfIdle` returning without blocking — keeps its own five seconds. | fixed |
| D17 | **App-relative parser discovery makes the test suite's fake parser look like a real one, so `RealParserFact` runs instead of skipping.** `PanGlossExecutable.TryLocate` prefers an executable beside the application — `AppContext.BaseDirectory` — before falling back to a development checkout. Under test that directory is the test output directory, and `tests/FakePanGloss` builds with `AssemblyName=pangloss`, so its `pangloss.exe` landed there and discovery returned it. `RealParserFactAttribute` then believes a genuine parser is present and runs the tests it gates, against the fake — which is precisely the outcome its own doc comment warns about: *"a green test proving nothing is worse than a skipped one, because it looks like coverage."* Every gate run so far set `MOTIF_PANGLOSS_EXE`, and a configured override wins over app-relative discovery, so the hazard stayed invisible on exactly the runs that would have caught it. Found while reviewing the release-surface lane, whose agent hit the symptom and deleted `$(TargetDir)pangloss.*` in a `BeforeTargets="VSTest"` target — treating the output rather than the copy. | **fixed** — the `ProjectReference` to `FakePanGloss` now carries `Private="false"`, so the fake never enters the test project's output and discovery has nothing to mistake. `FakeParser.ExecutablePath` reads FakePanGloss's own `bin` directory, so the tests that deliberately use the fake are unaffected. **What is not fixed** is the discovery rule itself: any `pangloss.exe` that happens to sit beside a Motif executable is still taken as the parser, with no check that it is the bundled one. R9 asks for exactly that check — verify the executable's described commands and expected evidence schema before a long run — so this is a reason to do it, not a separate defect. |
| D18 | **A blank LibLCM project is not a parsable project, and the difference surfaces as a panic string.** Generating a Handoff over a freshly created project fails in the parser with `UnsegmentableBoundary("table \"main\" cannot segment the morph boundary \"+\" ...; a table needs at least its boundary/phoneme definitions")`. `LcmCache.CreateCacheWithNewBlankLangProj` seeds no phonology, so every vernacular letter is unsegmentable, and with no compound rules PanGloss synthesises two defaults joined by a literal `+` that the empty phoneme table also cannot segment. The repository already knows both halves — `RealParserProject` exists to patch them for tests and documents them — but the consequence has not been drawn for the product: *a project LibLCM considers valid* and *a project PanGloss can parse a word against* are different states, and today a user meeting the second one gets a Rust panic rather than a refusal naming the missing phoneme set. R3 promises actionable refusals for unsupported projects; this is one of them. | open |
